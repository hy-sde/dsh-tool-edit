name: Bench rich (@hy-sde-org/dsh-tool-edit)
description: A/B bench arm mounting @hy-sde-org/dsh-tool-edit so its `edit` shadows the deployment's stock one — mount together with the `baseline` preset for the oh-my-pi edit benchmark.
order: 81
